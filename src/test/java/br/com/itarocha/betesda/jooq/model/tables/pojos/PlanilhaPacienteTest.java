package br.com.itarocha.betesda.jooq.model.tables.pojos;

//import br.com.itarocha.betesda.testes.PlanilhaPaciente;
import org.junit.jupiter.api.Test;
import pl.pojo.tester.api.assertion.Method;

import static pl.pojo.tester.api.assertion.Assertions.assertPojoMethodsFor;

class PlanilhaPacienteTest {

    /*
    @Test
    public void Should_Pass_All_Pojo_Tests() {
        // given
        final Class<?> classUnderTest = PlanilhaPaciente.class;

        // when

        // then
        //assertPojoMethodsFor(classUnderTest).areWellImplemented();

        assertPojoMethodsFor(classUnderTest).testing(Method.GETTER, Method.SETTER, Method.TO_STRING)
                .testing(Method.EQUALS)
                .testing(Method.HASH_CODE)
                .testing(Method.CONSTRUCTOR)
                .areWellImplemented();
    }
    */
}